name: Deploy Worker

on:
  workflow_dispatch:
  push:
    branches: ["main"]

jobs:
  deploy:
    uses: Autus-Solutions/autus-infra/.github/workflows/reusable-k8s-build-deploy.yml@main
    with:
      app_name: autus-worker
      namespace: autus
      dockerfile: ./WorkerDockerfile
      context: .
      container_port: "8080"
      health_path: /health
      ingress_hosts: ""
      environment_name: production
      replicas: "1"
      image_pull_secret_name: ghcr-pull-secret
      secret_name: autus-worker-secrets
    secrets:
      KUBE_CONFIG: ${{ secrets.KUBE_CONFIG }}
